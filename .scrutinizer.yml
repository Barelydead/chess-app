checks:
    javascript:
        code_rating: true

build:
    enviroment:
        node: 8
    tests:
        override:
            -
                command: 'npm run test-scrutinizer'
                coverage:
                    file: 'coverage/clover.xml'
                    format: 'clover'
